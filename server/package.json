{
    "name": "media-casting-system",
    "version": "1.0.0",
    "description": "A system for casting media between devices",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "dev": "nodemon server.js",
        "build": "cd client && npm run build",
        "install-client": "cd client && npm install",
        "heroku-postbuild": "npm run install-client && npm run build"
    },
    "dependencies": {
        "@ffmpeg-installer/ffmpeg": "^1.1.0",
        "child_process": "^1.0.2",
        "cors": "^2.8.5",
        "express": "^4.21.2",
        "fluent-ffmpeg": "^2.1.3",
        "fs": "^0.0.1-security",
        "multer": "^1.4.5-lts.2",
        "node-media-server": "^4.0.12",
        "path": "^0.12.7",
        "socket.io": "^4.8.1"
    },
    "devDependencies": {
        "nodemon": "^3.0.1"
    },
    "engines": {
        "node": "18.x"
    }
}
